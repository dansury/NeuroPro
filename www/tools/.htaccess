# Раздаём инструменты оператора как обычные файлы для скачивания: сервер их не
# исполняет, браузер получает Content-Type для «Сохранить как…».
<IfModule mod_mime.c>
    AddType application/octet-stream .cmd
</IfModule>
