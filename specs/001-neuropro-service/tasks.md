# Tasks: NeuroPro

Статус: `[x]` готово и проверено · `[~]` базово готово, нужна доводка · `[ ]` не начато.

## Фаза 1 — Слой данных (offline) ✅

- [x] T001 OLE2 + BIFF8 ридер `.xls` (`app/lib/excel.php`); xlsx + csv.
- [x] T002 SST с CONTINUE (кириллица), RK/MULRK/NUMBER/FORMULA+STRING.
- [x] T003 `Profile::fromFile()` — метаданные, тип теста, когнитивные баллы.
- [x] T004 Проверка на эталоне `Sources/…Дронов Андрей….xls` (12 баллов совпали).
- [x] T005 Короткие подписи осей (как в исходном отчёте).

## Фаза 2 — Диаграмма (математика) ✅

- [x] T010 `Chart::svg()` — кольца, оси, подписи, полигон когниции.
- [x] T011 Наложение физиологии (Зна вокруг медианы) вторым полигоном.
- [x] T012 Легенда, бренд‑цвета, Verdana; визуальная сверка с референсом.
- [x] T013 `Phys::parse()` — OCR‑текст → выровненные Зна (12/12 на тесте).

## Фаза 3 — Хранилище и промпты ✅

- [x] T020 Схема SQLite (`db.php`): profiles, prompts, prompt_versions, interpretations.
- [x] T021 `Prompts` — версии, активная версия, комментарии.
- [x] T022 Правила удаления: версия — только без интерпретаций; интерпретация — с подтверждением (UI).
- [x] T023 Сидинг промптов СМУ/LSI из `Sources/*.txt` (`bootstrap.php`).

## Фаза 4 — Нейросеть и OCR ✅/⚠️ (нужны ключи для live)

- [x] T030 `LLM::ocrImage()` — Yandex Vision OCR скриншота (код из Sources).
- [x] T031 `Interpret::run()` — сборка запроса + вызов + сохранение к версии.
- [~] T032 Живой прогон OCR + интерпретации (требует `YANDEX_API_KEY` /
  `OPENROUTER_API_KEY` в `setup.php`; путь кода покрыт смоуком, сеть не звалась).

## Фаза 5 — Веб‑приложение ✅

- [x] T040 Фронт‑контроллер `public/index.php` (роуты, layout, Verdana).
- [x] T041 Загрузка (файл / вставка текста) → профиль.
- [x] T042 Профиль: превью когнитивной диаграммы + вставка скриншота (Ctrl+V).
- [x] T043 Результат: диаграмма с физиологией + интерпретации + действия.
- [x] T044 Промпты: список версий, редактор (новая версия), активная, удаление.
- [x] T045 `setup.php` — провайдер/модель/OCR/SMTP (из Sources).
- [x] T046 HTTP‑смоук всех роутов (200, без фаталов).

## Фаза 6 — Выдача ✅/~

- [x] T050 `Report::html()` — брендированный отчёт (Verdana, лого, диаграмма,
  таблица баллов с группировкой достижение/отношение, контакты).
- [x] T051 Скачивание PDF через печать браузера (print‑CSS, A4, Verdana).
- [x] T052 Отправка письма клиенту (`Mailer::sendCustom`, нужен SMTP в настройках).
- [ ] T053 Серверный PDF с встроенным TTF Verdana (tFPDF/аналог) — для отдачи
  файла без участия браузера.

## Фаза 7 — Watch‑folder ✅

- [x] T060 `bin/watch.php` — слежение за папкой, идемпотентный ingest по sha1.
- [x] T061 Профиль «ожидает скриншот» на дашборде после авто‑подхвата.

## Фаза 8 — Методика Басса-Дарки ✅

- [x] T080 Детекция типа `bd` по строке «Методика» (`Profile::TEST_TYPES`);
  проверено на эталоне `Sources/Дронов Андрей Басса-Дарки 23-06-2026 13 22.xls`
  (8 баллов 9,7,7,3,5,8,11,3).
- [x] T081 Пошкальные максимумы (`Profile::BD_SCALE_MAX`, 10/9/11/5/8/10/13/9)
  и индексы агрессивности/враждебности с нормами (`Profile::bdIndices()`,
  математика — принцип I).
- [x] T082 Диаграмма для `bd`: шкала 0–13, кольцо на каждый балл, заголовок
  «Профиль агрессивности» (`Profile::chartTitle()`).
- [x] T083 Промпт `Sources/BD PROMPT.txt` (по образцу СМУ/ИЖС) + сидинг семейства
  `bd` в `bootstrap.php`.
- [x] T084 Отчёт: единица «Баллы» (а не «%») для не-LSI, блок «Заключение»
  с индексами для `bd`; в сообщение нейросети баллы идут как «N из max» + индексы.

## Остаётся (next)

- [ ] T070 LSI/ИЖС: получить реальный образец `.xls` и проверить парсер баллов
  (структура листа может отличаться от СМУ; % шкала).
- [ ] T071 Ручная правка выровненных физиологических Зна в UI перед отрисовкой
  (страховка от ошибок OCR; принцип II).
- [ ] T072 Аутентификация операторов для основного UI (сейчас защищён только
  `setup.php`).
- [ ] T073 Параметры распознавания/сопоставления параметров значимости для
  разных методик (словарь синонимов названий).
- [ ] T074 Юнит‑тесты на `excel`/`phys`/`chart` (вынести смоук в `tests/`).
